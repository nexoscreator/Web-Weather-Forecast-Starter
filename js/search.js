function handleFormSubmit(e){e.preventDefault();const t=document.getElementById("locationInput").value;fetchWeather(t)}document.getElementById("searchForm").addEventListener("submit",handleFormSubmit);